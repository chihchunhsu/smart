import smart
import numpy as np
from scipy.optimize import curve_fit
import matplotlib.pyplot as plt
import copy
import tellurics

#--------------------------------------------
# 20200223 chsu: routine to generate the mask 
#--------------------------------------------

def telluric_mask(telluric, sigma=2.5, lsf=4.8):
	"""
	"""
	pwv = 
	airmass = 

	# simple chi2 comparison with different pwv

	if diagnostic:
		plt.plot(telluric, )

